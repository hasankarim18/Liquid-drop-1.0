<div class="my_container">
  <h2 class="text-2xl">Cart page</h2>
  {% if cart.item_count > 0 %}
    cart table
  {% else %}
    <div>
      <h2 class="text-2xl mb-6 font-semibold text-center">
        Your cart is empty
      </h2>
      <div class="flex justify-center">
        <a class="btn btn-outline btn-lg" href="{{ routes.collections_url }}">Back to shopping</a>
      </div>
    </div>
  {% endif %}


</div>